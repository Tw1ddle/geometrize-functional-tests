sudo: required
dist: trusty
language: python

python:
  - "3.6"

# Setup build matrix
matrix:
  include:
  - os: linux
  - os: osx
      
before_script:
  -

# Run the tests, gather results etc
script:
  - python get_geometrize.py
  - python run_tests.py
  - python report_result.py